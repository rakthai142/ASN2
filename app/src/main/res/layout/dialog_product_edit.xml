<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/frameLayout2"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingHorizontal="12dp"
    tools:context=".sales.products.ProductEditFragment">

    <EditText
        android:id="@+id/prodEditBarcode"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Barcode"
        android:importantForAutofill="no"
        android:inputType="number"
        app:layout_constraintTop_toTopOf="parent" />

    <EditText
        android:id="@+id/prodEditName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Product name"
        android:importantForAutofill="no"
        android:inputType="textCapWords"
        app:layout_constraintTop_toBottomOf="@id/prodEditBarcode" />

    <EditText
        android:id="@+id/prodEditPriceSatang"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Price (satang)"
        android:importantForAutofill="no"
        android:inputType="number"
        app:layout_constraintTop_toBottomOf="@id/prodEditName" />

    <EditText
        android:id="@+id/prodEditCategory"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Category"
        android:importantForAutofill="no"
        android:inputType="text"
        app:layout_constraintTop_toBottomOf="@id/prodEditPriceSatang" />

</androidx.constraintlayout.widget.ConstraintLayout>